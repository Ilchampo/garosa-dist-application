export enum LogStatus {
    SUCCESSS = 0,
    FAILED = 1,
}

export enum RoutePointStatus {
    ASSIGNED = 0,
    IN_PROGRESS = 1,
    FINISHED = 2,
}

export enum Roles {
    INVALID = 0,
    ADMINISTRATOR = 1,
    SUPERVISOR = 2,
    DISTRIBUTOR = 3,
}
